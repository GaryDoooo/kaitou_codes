#include <iostream>
#include <vector>
using namespace std;
int main() {
    int n, k;
    cin >> n >> k;
    vector<int> a;
    int x;
    while (cin >> x) {
        a.push_back(x);
    }
    vector<int> b;
    for (int i = 0; i < x.size(); i++) {
    }
}