#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main() {
    vector<int> scores;
    int x;
    while (cin >> x) {
        scores.push_back(scores);
    }
    sort(scores.begin(), scores.end())
    for (int i = 0; i < 3; i++)
    }